.single-container
  .ser
    %header.single-header
      %h1
        -# = link_to image_tag("logo.svg", class:"img"), root_path
      %nav.progress
        %ul.progress-bar
          %li.through
            会員情報
            .progress-status#active
            .progress-status_bar#active
          %li.active#red
            電話番号認証
            .progress-status#active
            .progress-status_bar
          %li.address#address
            お届け先住所入力
            .progress-status
            .progress-status_bar
          %li.payment#payment
            支払い方法
            .progress-status
            .progress-status_bar
          %li.end#end
            完了
            .progress-status
            .progress-status_bar
  %main.single-main
    .l-single-container
      %h2.l-chapter-head.registration
        電話番号の確認
      .l-single-inner.registration-form
        = form_for @user, url:phone_authentication_path, method: :get do |f|
          %input{name: "after_save_callback", type: "hidden", value: ""}
          .l-single-content
            .form-group
              %label{for: "phone_number"} 携帯電話の番号
              = f.text_field :phone_number, placeholder: "携帯電話の番号を入力", type:"tel", value:"", class:"input-default2"
            %p 本人確認のため、携帯電話のSMS(ショートメッセージサービス)を利用して認証を行います。
            = f.submit "SMSを送信する", class:"btn-default btn-red"
            %p ※電話番号は本人確認や不正利用防止のために利用します。他のユーザーに公開されることはありません。
            .form-group.text-right
              = link_to "電話番号が必要な理由" ,"https://www.mercari.com/jp/help_center/",class:"link_decoration2"
              -# = fa_icon "chevron-right", class:"rightIcon2"
  -# = render "footer"